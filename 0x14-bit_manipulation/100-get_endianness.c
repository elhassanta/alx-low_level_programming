#include "main.h"

/**
 *get_endianness - this function deteminate how bytes storeed
 *Return: return an integer 0 or 1
 */
int get_endianness(void)
{
	int i = 1;
	int b = (char)i;

	if (b)
		return (b);
	return (b);
}
